name: All the CI

on:
  push:
    branches:
      - master
  pull_request: {}
jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: dcodeIO/setup-node-nvm@v4.0.0
    - name: Unit Tests
      run: |
        npm ci
        npm run test
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: dcodeIO/setup-node-nvm@v4.0.0
    - name: Lint
      run: |
        npm ci
        npm run lint
